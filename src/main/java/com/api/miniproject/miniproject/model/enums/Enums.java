package com.api.miniproject.miniproject.model.enums;

public class Enums {
    public enum Roles {
        AUTHOR,
        READER,
    }

    public enum Category {
        categoryId,
        name,
        amountOfArticles,
        createdAt,
        updatedAt
    }

    public enum Article {
        articleId,
        title,
        description,
        createdAt,
        updatedAt
    }
}
